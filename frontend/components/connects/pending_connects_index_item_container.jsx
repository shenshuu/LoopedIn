import { connect } from "react-redux";
import PendingConnectsIndexItem from './pending_connects_index_item';

const mapStateToProps = state => ({

});

const mapDispatchToProps = dispatch => ({

});

export default connect(mapDispatchToProps, mapDispatchToProps)(PendingConnectsIndexItem);